# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
release
/release-build/
/InterAct.exe
/InterAct.zip
# Locally extracted zip build kept around to run directly.
/InterAct/
.codex-tmp/
supabase/.temp/
.env
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# electron-builder stages here before renaming into place. On this machine the
# rename sometimes fails while Dropbox is watching the folder, leaving 350MB of
# Electron binaries at the repository root for `git add -A` to sweep up.
/win-unpacked.tmp/
